<!-- Generated by scripts/sync.py. Do not edit by hand: the next sync overwrites it. -->

# Web Crawler

<!-- meta:begin -->
| Format | Difficulty | Asked at | Round | Topics | Last reported |
| --- | --- | --- | --- | --- | --- |
| AI Coding | Medium | Anthropic | Phone screen, Onsite / virtual onsite | — | May 2026 |
<!-- meta:end -->

> **▶ [Solve it on TrueInterview](https://trueinterview.io/questions/web-crawler)** — free, no card: the interview workspace, an AI interviewer to push back on your design, and the reference solution.

## Problem

## Web Crawler

CodingPhone, OnsiteSoftware EngineerReported Apr, 2026High Frequency

### Basic Problem

You receive:

* An initial URL, `startUrl`
* An `HtmlParser` interface that retrieves every URL linked from a specified page

Build a crawler that **returns every URL reachable from `startUrl` whose hostname is the same as `startUrl`**. The result may be returned in any order.

The `HtmlParser` interface is defined below:

```
interface HtmlParser {
    // Returns all URLs from a given page URL.
    public List<String> getUrls(String url);
}
```

The function is invoked as follows:

```
List<String> crawl(String startUrl, HtmlParser htmlParser)
```

#### Requirements

The crawler must:

1. Begin with `startUrl`.
2. Call `HtmlParser.getUrls(url)` to retrieve the links on a page.
3. Prevent duplicate crawling by ensuring that no URL is crawled more than once.
4. Follow only URLs whose **hostname** equals the hostname of `startUrl`.
5. Treat every URL as using the `http` protocol and as having no port.

#### Note for Candidate

* **URL Fragments**: Decide how fragments such as `http://example.com/page#section1` should be treated. Should that URL differ from `http://example.com/page#section2`, or should both identify one URL? Discuss the choice with the interviewer if clarification is necessary.
* **URL Normalization**: For the basic version, you may assume that URL normalization is unnecessary.

### Follow-up: Multithreaded/Concurrent Implementation (Important!!)

Once the single-threaded crawler is complete, create a multithreaded or concurrent version that can improve performance.

#### Requirements

The concurrent crawler must:

1. Fetch multiple URLs in parallel.
2. Protect shared structures, including the visited set and result list, from race conditions.
3. Ensure that concurrent execution still crawls each URL at most once.
4. Continue limiting traversal to URLs with the same hostname.

#### Note for Candidate

Use a thread pool to cap the number of active threads.

* Avoid creating an unlimited number of threads, such as one thread for every URL.
* A thread pool bounds concurrency and helps prevent resource exhaustion.
* A typical design uses a fixed-size pool, for example with 10-20 threads, together with a task queue.

## Hints

<details>
<summary>Hint 1</summary>

Start from the given URL and use BFS or DFS to explore links, but only follow those with the same hostname as the start URL.

</details>

<details>
<summary>Hint 2</summary>

Parse the hostname from the start URL once, then for each new URL, extract its hostname and compare — you can use a simple string operation like splitting by '/' or using a URL parser.

</details>

<details>
<summary>Hint 3</summary>

Be careful to avoid revisiting the same URL, use a set to track visited URLs, and consider that the same page might be linked multiple times.

</details>

## Solution

The reference solution is on [the question page](https://trueinterview.io/questions/web-crawler), beside an editor that runs your own attempt first. It is not reproduced here — try the problem before you read the answer.

## Asked at

[Anthropic](../../../companies/anthropic/README.md)

---

<sub>From [TrueInterview](https://trueinterview.io) — the free part of its question bank and company interview guides, synced every hour. Text licensed [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/): share or adapt it anywhere, crediting TrueInterview with a link back ([how](../../../LICENSE-CONTENT.md)). [Report a problem with this page](https://github.com/kevin-2023-code/Tech-Interview-Questions/issues/new?template=correction.yml).</sub>
