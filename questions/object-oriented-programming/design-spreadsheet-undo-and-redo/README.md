<!-- Generated by scripts/sync.py. Do not edit by hand: the next sync overwrites it. -->

# Design Spreadsheet Undo and Redo

<!-- meta:begin -->
| Format | Difficulty | Asked at | Round | Topics | Last reported |
| --- | --- | --- | --- | --- | --- |
| Object Oriented Programming | Medium | Airtable | Onsite / virtual onsite | — | — |
<!-- meta:end -->

> **▶ [Solve it on TrueInterview](https://trueinterview.io/questions/1855e2e5-c079-5b9d-86be-33ea9b14c2ce)** — free, no card: a runnable editor, the sample and hidden tests, a judged verdict, and the reference solution.

## Problem

## Problem: Build a Spreadsheet Undo/Redo Manager

Create an edit-history manager for a basic spreadsheet whose cells all begin empty. Operations are processed in order, and the user may reverse the most recent edit that has not already been reversed or restore the most recent edit that was reversed.

The initial version changes only one cell per edit:

- `SET cell value`: Assign the specified value to the cell.
- `UNDO`: Reverse the newest undoable edit; do nothing when no such edit exists.
- `REDO`: Reapply the newest redoable edit; do nothing when no such edit exists.

Rules:

1. Any new `SET` operation discards every pending redo operation.
2. Repeated edits to one cell must be reversed and restored in their proper sequence.
3. `GET cell` reports the cell's current value. If the cell has never received a value or is empty now, return an empty string.
4. Implement:

```python
process(operations: list[str]) -> list[str]
```

The returned list must contain the results of all `GET` operations, in the order those queries occur.

### Example 1

```text
Input:
[
  "SET A1 hello",
  "SET A1 world",
  "UNDO",
  "GET A1",
  "REDO",
  "GET A1"
]

Output:
["hello", "world"]
```

Undo restores `A1` to `hello`, and redo applies the second assignment again, producing `world`.

### Example 2

```text
Input:
[
  "SET A1 1",
  "SET B1 2",
  "UNDO",
  "SET C1 3",
  "REDO",
  "GET B1",
  "GET C1"
]

Output:
["", "3"]
```

The new assignment to `C1` removes the pending redo for `B1`, so `REDO` has no effect; `B1` is empty and `C1` contains `3`.

### Constraints

- `1 <= len(operations) <= 2 * 10^5`
- A cell name contains uppercase letters and digits, such as `A1` or `BC27`.
- A `SET` value contains no newline characters and has length at most `10^4`.
- Aim for amortized `O(1)` processing per operation. Do not duplicate the complete spreadsheet state for every edit.

## Solution

The reference solution is on [the question page](https://trueinterview.io/questions/1855e2e5-c079-5b9d-86be-33ea9b14c2ce), beside an editor that runs your own attempt first. It is not reproduced here — try the problem before you read the answer.

## Asked at

[Airtable](../../../companies/airtable/README.md)

---

<sub>From [TrueInterview](https://trueinterview.io) — the free part of its question bank and company interview guides, synced every hour. Text licensed [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/): share or adapt it anywhere, crediting TrueInterview with a link back ([how](../../../LICENSE-CONTENT.md)). [Report a problem with this page](https://github.com/kevin-2023-code/Tech-Interview-Questions/issues/new?template=correction.yml).</sub>
