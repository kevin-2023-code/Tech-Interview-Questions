<!-- Generated by scripts/sync.py. Do not edit by hand: the next sync overwrites it. -->

# Binary Search Tree Iterator

<!-- meta:begin -->
| Format | Difficulty | Asked at | Round | Topics | Last reported |
| --- | --- | --- | --- | --- | --- |
| Object Oriented Programming | Medium | Apple · Microsoft | Phone screen | trees | Jan 2026 |
<!-- meta:end -->

> **▶ [Solve it on TrueInterview](https://trueinterview.io/questions/binary-search-tree-iterator)** — free, no card: a runnable editor, the sample and hidden tests, a judged verdict, and the reference solution.

## Problem

## Binary Search Tree In-Order Iterator

Create a `BSTIterator` class that walks through the values of a binary search tree (BST) in in-order sequence:

```text
BSTIterator(TreeNode root)
boolean hasNext()
int next()
```

- `BSTIterator(TreeNode root)` constructs the iterator using `root` as the tree's root. Before iteration begins, its pointer is positioned conceptually at a value that does not exist and is less than every value in the tree.
- `boolean hasNext()` reports whether another value appears after the current pointer position. Return `true` when one exists and `false` otherwise.
- `int next()` advances the pointer by one position in the traversal and returns the value reached.

Because iteration starts below every tree value, the first call to `next()` produces the smallest value in the BST.

You may rely on every call to `next()` being valid: whenever it is invoked, another value remains in the in-order traversal.

Aim for an implementation that requires `O(h)` additional memory, where `h` denotes the height of the tree.

### Examples

Example 1:

```text
Input:
["BSTIterator", "next", "hasNext", "next", "next", "hasNext", "next", "hasNext", "next", "hasNext"]
[[[10, 4, 18, null, null, 13, 21]], [], [], [], [], [], [], [], [], []]

Output: [null, 4, true, 10, 13, true, 18, true, 21, false]
```

Explanation: The tree's in-order sequence is `4, 10, 13, 18, 21`, so each `next()` returns the next value in that order and `hasNext()` becomes `false` only after the final value.

### Constraints

- The tree contains between `1` and `10^5` nodes, inclusive.
- `0 <= Node.val <= 10^6`
- Across the entire usage of the iterator, there are no more than `10^5` calls to `hasNext` and `next` combined.

## Hints

<details>
<summary>Hint 1</summary>

Simulate the recursive in-order traversal using an explicit stack to avoid recursion.

</details>

<details>
<summary>Hint 2</summary>

The stack should hold nodes whose left subtree has been fully processed, so you can pop and yield the node, then push its right child.

</details>

<details>
<summary>Hint 3</summary>

Both `hasNext()` and `next()` should run in O(1) amortized time, and the space complexity is O(h) where h is the tree height.

</details>

## Solution

The reference solution is on [the question page](https://trueinterview.io/questions/binary-search-tree-iterator), beside an editor that runs your own attempt first. It is not reproduced here — try the problem before you read the answer.

## Asked at

[Apple](../../../companies/apple/README.md) · [Microsoft](../../../companies/microsoft/README.md)

---

<sub>From [TrueInterview](https://trueinterview.io) — the free part of its question bank and company interview guides, synced every hour. Text licensed [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/): share or adapt it anywhere, crediting TrueInterview with a link back ([how](../../../LICENSE-CONTENT.md)). [Report a problem with this page](https://github.com/kevin-2023-code/Tech-Interview-Questions/issues/new?template=correction.yml).</sub>
