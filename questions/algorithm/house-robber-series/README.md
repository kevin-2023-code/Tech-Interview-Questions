<!-- Generated by scripts/sync.py. Do not edit by hand: the next sync overwrites it. -->

# House Robber Series

<!-- meta:begin -->
| Format | Difficulty | Asked at | Round | Topics | Last reported |
| --- | --- | --- | --- | --- | --- |
| Algorithm | Medium | Databricks · Amazon · Expedia | Phone screen, Onsite / virtual onsite | dynamic-programming | Nov 2025 |
<!-- meta:end -->

> **▶ [Solve it on TrueInterview](https://trueinterview.io/questions/house-robber-series)** — free, no card: a runnable editor, the sample and hidden tests, a judged verdict, and the reference solution.

## Problem

### Overview

Picture yourself as a professional robber eyeing a row of houses. Each house holds a known stash of cash (a non‑negative integer). The neighbourhood alarm is wired so that **you cannot rob two houses that sit too close together** – doing so would trigger the system. The challenge is to maximise your total haul while respecting the spacing rule.

The problem comes in several flavours:

- The basic version forbids **adjacent** houses.
- If the street forms a circle, the first and last house become neighbours as well.
- A tunable gap `k` generalises the rule: robbing house `i` makes every house up to `k` steps away off‑limits (`k = 1` means only immediate neighbours).

You need to write a single function that handles all these scenarios:

```python
def rob(nums: List[int], k: int, circular: bool) -> int:
    ...
```

- **`nums`** – a list of non‑negative integers; the amount of money stored in each house.
- **`k`** – the minimum safe gap between robbed houses. If you rob house `i`, you may not rob any house with index in `[i+1, i+k]`.
- **`circular`** – when `True`, houses are arranged in a circle, so house `0` and house `n-1` are adjacent and cannot both be robbed.

---

### Basic Scenario (`k = 1, circular = False`)

This is the original “House Robber” problem. With `k = 1` and `circular = False`, the only restriction is that you may not target two neighbouring houses.

#### Examples

```
# Example 1
nums = [1, 2, 3, 1]
# Output: 4
# Explanation: Take houses at indices 0 and 2 (1 and 3) → total 4.

# Example 2
nums = [2, 7, 9, 3, 1]
# Output: 12
# Explanation: Take houses at indices 0, 2, and 4 (2, 9, and 1) → total 12.

# Example 3
nums = [5, 3, 4, 11, 2]
# Output: 16
# Explanation: Take houses at indices 0 and 3 (5 and 11) → total 16.
```

```trueviz
{"v":1,"kind":"example","title":"House Robber Basic Example","input":{"nums":"[1, 2, 3, 1]","k":"1","circular":"false"},"steps":[{"say":"We have 4 houses with money amounts: [1, 2, 3, 1].","panels":[{"type":"array","id":"houses","label":"Houses","values":["1","2","3","1"]}]},{"say":"We need to select houses to rob, but cannot rob two adjacent houses. Let's consider robbing house 0 (value 1).","panels":[{"type":"array","id":"houses","label":"Houses","values":["1","2","3","1"],"marks":{"read":[0]}}]},{"say":"House 1 (value 2) is adjacent to house 0, so it becomes off-limits.","panels":[{"type":"array","id":"houses","label":"Houses","values":["1","2","3","1"],"marks":{"read":[0],"dim":[1]}}]},{"say":"We can rob house 2 (value 3) because it is not adjacent to house 0 (distance 2).","panels":[{"type":"array","id":"houses","label":"Houses","values":["1","2","3","1"],"marks":{"read":[0,2],"dim":[1]}}]},{"say":"House 3 (value 1) is adjacent to house 2, so it's off-limits.","panels":[{"type":"array","id":"houses","label":"Houses","values":["1","2","3","1"],"marks":{"read":[0,2],"dim":[1,3]}}]},{"say":"Total money from this selection: 1 + 3 = 4. This is a candidate.","panels":[{"type":"array","id":"houses","label":"Houses","values":["1","2","3","1"],"marks":{"found":[0,2]}}]},{"say":"Other combinations (like robbing houses 1 and 3 for 3) yield less. So 4 is the maximum.","panels":[{"type":"array","id":"houses","label":"Houses","values":["1","2","3","1"],"marks":{"found":[0,2]}}]}],"expected":"4"}
```

#### Constraints

- `1 <= len(nums) <= 100`
- `0 <= nums[i] <= 400`

---

### Follow‑up 1: Circular Houses

When `circular = True` (and `k = 1`), the first and last homes are side‑by‑side. Consequently, you cannot rob both house `0` and house `n-1`.

#### Examples

```
# Example 1
nums = [2, 3, 2]
# Output: 3
# Explanation: House 0 and house 2 are adjacent in the circle.
# Safest to rob only house 1 → 3.

# Example 2
nums = [1, 2, 3, 1]
# Output: 4
# Explanation: Rob houses 0 and 2 → 1 + 3 = 4.

# Example 3
nums = [1, 2, 3]
# Output: 3
# Explanation: Pick house 2 alone → 3.
```

The same base constraints on `nums` apply.

---

### Follow‑up 2: Adjustable Gap `k`

Generalise the spacing rule. When `circular = False`, robbing house `i` bans houses `i+1`, `i+2`, …, `i+k`. With `k = 1` you get the original adjacent‑only behaviour; larger `k` enforce a wider safety buffer.

#### Examples

```
# k = 1 (reduces to basic)
nums = [2, 7, 9, 3, 1]
# Output: 12
# Explanation: Rob indices 0, 2, 4 → 2 + 9 + 1 = 12.

# k = 2 (must leave at least two empty houses after a hit)
nums = [5, 1, 3, 6, 7]
# Output: 12
# Explanation: Steal from house 0 (5) then house 4 (7) → 5 + 7 = 12.
# Other combinations: (0,3)=11, (1,4)=8.
```

The same base constraints apply. In addition, you may assume `1 <= k < n` for non‑trivial cases.

---

### Additional Follow‑up: House Robber III (Tree)

In this variant the houses are arranged as a **binary tree**. Every node stores a monetary value. Robbing two nodes that are directly linked (parent‑child) trips the alarm.

Write a function appropriate for the tree input that returns the maximum amount you can collect without activating the alarm.

#### Examples

```
# Example 1
# Tree:
#     3
#    / \
#   4   5
# Output: 9
# Explanation: Skip the root, rob both children: 4 + 5 = 9.
# Taking the root gives only 3 (children become inaccessible).

# Example 2
# Tree:
#     2
#    / \
#   1   3
#  /
# 4
# Output: 7
# Explanation: Rob leaf 4 and right child 3 (neither is a parent/child pair) → 4 + 3 = 7.
# Alternates: root + leaf = 6, children only = 4.
```

## Hints

<details>
<summary>Hint 1</summary>

The problem reduces to a classic DP where you decide to rob or skip each house based on previous decisions.

</details>

<details>
<summary>Hint 2</summary>

Use two variables to track the max profit up to the previous house and the one before that, updating iteratively.

</details>

## Solution

The reference solution is on [the question page](https://trueinterview.io/questions/house-robber-series), beside an editor that runs your own attempt first. It is not reproduced here — try the problem before you read the answer.

## Asked at

[Databricks](../../../companies/databricks/README.md) · [Amazon](../../../companies/amazon/README.md) · [Expedia](../../../companies/expedia/README.md)

---

<sub>From [TrueInterview](https://trueinterview.io) — the free part of its question bank and company interview guides, synced every hour. Text licensed [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/): share or adapt it anywhere, crediting TrueInterview with a link back ([how](../../../LICENSE-CONTENT.md)). [Report a problem with this page](https://github.com/kevin-2023-code/Tech-Interview-Questions/issues/new?template=correction.yml).</sub>
