<!-- Generated by scripts/sync.py. Do not edit by hand: the next sync overwrites it. -->

# Evaluate String Expression

<!-- meta:begin -->
| Format | Difficulty | Asked at | Round | Topics | Last reported |
| --- | --- | --- | --- | --- | --- |
| Algorithm | Medium | Google · Affirm · Uber | Onsite / virtual onsite | — | Jun 2026 |
<!-- meta:end -->

> **▶ [Solve it on TrueInterview](https://trueinterview.io/questions/nested-function-expression-evaluator)** — free, no card: a runnable editor, the sample and hidden tests, a judged verdict, and the reference solution.

## Problem

Basic Calculator with Nested Functions
Easy · Topics · Company Tags · Hints

Implement the function `evaluate(expression)` that computes the integer result of a string expression. The expression uses two named operations: `"add"` adds together all of its arguments, while `"sub"` subtracts the second argument from the first. The operations may be nested arbitrarily.

The expression may contain negative integers and spaces; the input is always syntactically valid.

Example 1:

```text
Input: "add(add(1,3), sub(1,3))"
Output: 2
```

Explanation: It computes (1 + 3) + (1 - 3) = 2.

Example 2:

```text
Input: "sub(1,3)"
Output: -2
```

Example 3:

```text
Input: "add(-1, 3)"
Output: 2
```

Constraints:

* The expression string consists only of `"add"`, `"sub"` functions, integer numbers, parentheses `"()"`, commas `","`, and spaces.
* The expression is always valid.
* All numbers in the expression are integers within the range [-10<sup>5</sup>, 10<sup>5</sup>].

## Hints

<details>
<summary>Hint 1</summary>

Consider parsing the expression recursively, where each function call is evaluated by first evaluating its arguments.

</details>

<details>
<summary>Hint 2</summary>

Use a stack to handle nested calls and operator precedence, or implement a recursive descent parser.

</details>

<details>
<summary>Hint 3</summary>

Watch out for division by zero and integer vs. floating-point results in the follow-up.

</details>

## Solution

The reference solution is on [the question page](https://trueinterview.io/questions/nested-function-expression-evaluator), beside an editor that runs your own attempt first. It is not reproduced here — try the problem before you read the answer.

## Asked at

[Google](../../../companies/google/README.md) · [Affirm](../../../companies/affirm/README.md) · [Uber](../../../companies/uber/README.md)

---

<sub>From [TrueInterview](https://trueinterview.io) — the free part of its question bank and company interview guides, synced every hour. Text licensed [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/): share or adapt it anywhere, crediting TrueInterview with a link back ([how](../../../LICENSE-CONTENT.md)). [Report a problem with this page](https://github.com/kevin-2023-code/Tech-Interview-Questions/issues/new?template=correction.yml).</sub>
