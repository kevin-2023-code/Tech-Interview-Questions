<!-- Generated by scripts/sync.py. Do not edit by hand: the next sync overwrites it. -->

# Hangman Optimal Strategy

<!-- meta:begin -->
| Format | Difficulty | Asked at | Round | Topics | Last reported |
| --- | --- | --- | --- | --- | --- |
| Algorithm | Hard | Dropbox | Phone screen | — | — |
<!-- meta:end -->

> **▶ [Solve it on TrueInterview](https://trueinterview.io/questions/03d208eb-7407-4966-b8c0-0f063d5fa252)** — free, no card: a runnable editor, the sample and hidden tests, a judged verdict, and the reference solution.

## Problem

## Problem

Two players face off in a game of Hangman. Player 2 secretly picks a word from a known list. Player 1, the guesser, does not know which word was chosen. On each turn, Player 1 guesses a single letter. Player 2 must reveal every occurrence of that letter in the secret word (if any), or declare the letter a miss. The guesser’s goal is to identify the secret word while incurring as few misses as possible.

Both players act optimally: Player 2 selects a word that forces the worst-case number of misses, and Player 1 chooses letters to minimize that worst-case count. Design an algorithm that, given the list of candidate words, returns the sequence of letter guesses Player 1 should make to guarantee the minimum possible number of incorrect guesses.

## Input

- `word_list: List[str]` – a non‑empty list of candidate words. All words have the same length and consist only of lowercase English letters.

## Output

- `List[str]` – the ordered sequence of letters that Player 1 should guess, following an optimal strategy. The sequence must minimize the maximum number of misses across all possible secret words.

## Constraints

- `1 <= len(word_list) <= 2000`
- `1 <= word_length <= 20`
- Words contain only characters `'a'`–`'z'`.
- A letter may be guessed at most once; the guesser never repeats a letter.

## Examples

### Example 1

```
Input:  word_list = ['apple', 'apply', 'angle']
Output: ['p', 'e']
```

**Explanation:**
- Guess `'p'`: present in `'apple'` and `'apply'`, but absent from `'angle'`. If it is a miss, the secret word is identified as `'angle'`.
- If `'p'` is present, guess `'e'`: present in `'apple'` and absent from `'apply'`. Thus the word is identified with at most 1 miss.
- Zero misses are impossible because distinguishing the words requires guessing a letter absent from at least one candidate.


### Example 2

```
Input:  word_list = ['cat', 'bat', 'rat']
Output: ['a', 't', 'c', 'b']
```

**Explanation:**
- Guess `'a'`: present in all words → 0 misses. Board: `_ a _`.
- Guess `'t'`: present in all words → 0 misses. Board: `_ a t`.
- Guess `'c'`: present only in `'cat'`. If the secret is `'cat'`, the word is identified; otherwise, this is one miss and the remaining possibilities are `'bat'` and `'rat'`.
- Guess `'b'`: if present, the word is `'bat'`; if absent, this is a second miss and the word must be `'rat'`. Thus at most 2 misses are needed, which is optimal.

## Hints

<details>
<summary>Hint 1</summary>

Model the game as a decision tree where Player 1 chooses letters to maximize information gain, while Player 2 chooses words to minimize it.

</details>

<details>
<summary>Hint 2</summary>

Use a minimax approach: at each turn, consider all possible secret words consistent with revealed letters, and pick the guess that minimizes the worst-case remaining possibilities.

</details>

<details>
<summary>Hint 3</summary>

The complexity target is O(N * L * 26) where N is number of words and L is word length, but careful with state representation.

</details>

## Solution

The reference solution is on [the question page](https://trueinterview.io/questions/03d208eb-7407-4966-b8c0-0f063d5fa252), beside an editor that runs your own attempt first. It is not reproduced here — try the problem before you read the answer.

## Asked at

[Dropbox](../../../companies/dropbox/README.md)

---

<sub>From [TrueInterview](https://trueinterview.io) — the free part of its question bank and company interview guides, synced every hour. Text licensed [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/): share or adapt it anywhere, crediting TrueInterview with a link back ([how](../../../LICENSE-CONTENT.md)). [Report a problem with this page](https://github.com/kevin-2023-code/Tech-Interview-Questions/issues/new?template=correction.yml).</sub>
