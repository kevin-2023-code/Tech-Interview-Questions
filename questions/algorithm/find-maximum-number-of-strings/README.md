<!-- Generated by scripts/sync.py. Do not edit by hand: the next sync overwrites it. -->

# Find Maximum Number of Strings

<!-- meta:begin -->
| Format | Difficulty | Asked at | Round | Topics | Last reported |
| --- | --- | --- | --- | --- | --- |
| Algorithm | Medium | Moveworks | Phone screen | backtracking | — |
<!-- meta:end -->

> **▶ [Solve it on TrueInterview](https://trueinterview.io/questions/3e15b4f6-57e4-466f-a91d-f7ce9dd81019)** — free, no card: a runnable editor, the sample and hidden tests, a judged verdict, and the reference solution.

## Problem

Maximum Number of Adjacent-Difference Bounded Strings
Medium · Topics · Company Tags · Hints

You are given two integers `n` and `k`. Your task is to compute the maximum number of strings of length `n` that can be formed under the following rules:

- Every character must be a lowercase English letter (`'a'` through `'z'`).
- For any two adjacent characters in the string, the absolute difference of their alphabetical indices must not exceed `k`.

The alphabetical index of a letter is its 1‑based position: `'a'` = 1, `'b'` = 2, …, `'z'` = 26. The difference between two letters `c1` and `c2` is `|pos(c1) - pos(c2)|`. For instance, `|pos('f') - pos('c')| = |6 - 3| = 3`, and `|pos('b') - pos('a')| = |2 - 1| = 1`.

Return the total count modulo `10^9 + 7` (since the answer can be very large).

Example 1:

```text
Input: n = 2, k = 3

Output: 170
```

Explanation: For length 2 with allowed adjacent difference ≤ 3, exactly 170 distinct strings over lowercase letters satisfy the condition.

Example 2:

```text
Input: n = 1, k = 10

Output: 26
```

Explanation: A string of length 1 has no adjacent pairs, so every lowercase letter is valid, giving 26 possible strings.

Example 3:

```text
Input: n = 3, k = 1

Output: 98
```

Constraints:

* `1 <= n <= 1,000,000`
* `1 <= k <= 25`
* The result must be returned modulo `1,000,000,007`.

## Hints

<details>
<summary>Hint 1</summary>

Think of building a string as walking n-1 steps on a graph whose 26 nodes are the lowercase letters.

</details>

<details>
<summary>Hint 2</summary>

Use dynamic programming by length and last letter, summing over all previous letters whose difference is allowed.

</details>

<details>
<summary>Hint 3</summary>

Use prefix sums over the 26 letters to update each row quickly, and remember that when k is large enough every string of length n is valid.

</details>

## Solution

The reference solution is on [the question page](https://trueinterview.io/questions/3e15b4f6-57e4-466f-a91d-f7ce9dd81019), beside an editor that runs your own attempt first. It is not reproduced here — try the problem before you read the answer.

## Asked at

[Moveworks](../../../companies/moveworks/README.md)

---

<sub>From [TrueInterview](https://trueinterview.io) — the free part of its question bank and company interview guides, synced every hour. Text licensed [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/): share or adapt it anywhere, crediting TrueInterview with a link back ([how](../../../LICENSE-CONTENT.md)). [Report a problem with this page](https://github.com/kevin-2023-code/Tech-Interview-Questions/issues/new?template=correction.yml).</sub>
