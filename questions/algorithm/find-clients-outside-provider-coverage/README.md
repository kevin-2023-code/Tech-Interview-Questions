<!-- Generated by scripts/sync.py. Do not edit by hand: the next sync overwrites it. -->

# Find Clients Outside Provider Coverage

<!-- meta:begin -->
| Format | Difficulty | Asked at | Round | Topics | Last reported |
| --- | --- | --- | --- | --- | --- |
| Algorithm | Medium | Oscar Health | Phone screen | — | — |
<!-- meta:end -->

> **▶ [Solve it on TrueInterview](https://trueinterview.io/questions/77313b88-1a9b-5047-b402-fced2dc1919c)** — free, no card: a runnable editor, the sample and hidden tests, a judged verdict, and the reference solution.

## Problem

Given collections of healthcare providers and clients, along with a maximum allowed service distance `maxDistance`, return the IDs of every client who is **not fully covered**.

Each provider has:

- `providerId: string`
- `specialties: string[]`: the services that provider supplies
- `location: [x, y]`: the provider's position in a two-dimensional coordinate system

Each client has:

- `clientId: string`
- `requirements: string[]`: the services the client needs
- `location: [x, y]`: the client's position in a two-dimensional coordinate system

For points `(x1, y1)` and `(x2, y2)`, calculate Euclidean distance as follows:

`distance = sqrt((x1 - x2)^2 + (y1 - y2)^2)`

A client's individual requirement is covered when there is at least one provider that satisfies both conditions:

1. The provider lists that service in `specialties`.
2. The provider is no farther than `maxDistance` from the client.

A client belongs in the result if any one of its requirements cannot be covered. Separate requirements for the same client may be fulfilled by separate providers. Providers and clients are allowed to share a location.

### Example

```text
Providers:
101, [ortho, cardio, pedi], [1, 2]
102, [pedi], [3, 4]
103, [ortho, cardio, pedi], [1, 8]
104, [ortho], [4, 2]
105, [ortho, pedi], [5, 1]

Clients:
201, [ortho, cardio], [4, 1]
202, [ortho], [3, 2]
203, [pedi], [5, 5]
204, [ortho, pedi], [2, 2]
205, [cardio], [3, 3]

maxDistance = 3
```

The result is:

```text
[203]
```

### Constraints

- `1 <= P, C <= 10^5`, with `P` representing the provider count and `C` representing the client count
- Every `specialties` or `requirements` list contains between `1` and `20` entries
- Coordinates are finite-precision floating-point values
- `0 <= maxDistance <= 10^9`

### Example

Input:

```text
5 5 3
101 ortho,cardio,pedi 1 2
102 pedi 3 4
103 ortho,cardio,pedi 1 8
104 ortho 4 2
105 ortho,pedi 5 1
201 ortho,cardio 4 1
202 ortho 3 2
203 pedi 5 5
204 ortho,pedi 2 2
205 cardio 3 3
```

Output:

```text
203
```

## Solution

The reference solution is on [the question page](https://trueinterview.io/questions/77313b88-1a9b-5047-b402-fced2dc1919c), beside an editor that runs your own attempt first. It is not reproduced here — try the problem before you read the answer.

## Asked at

[Oscar Health](../../../companies/oscar-health/README.md)

---

<sub>From [TrueInterview](https://trueinterview.io) — the free part of its question bank and company interview guides, synced every hour. Text licensed [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/): share or adapt it anywhere, crediting TrueInterview with a link back ([how](../../../LICENSE-CONTENT.md)). [Report a problem with this page](https://github.com/kevin-2023-code/Tech-Interview-Questions/issues/new?template=correction.yml).</sub>
