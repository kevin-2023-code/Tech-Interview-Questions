<!-- Generated by scripts/sync.py. Do not edit by hand: the next sync overwrites it. -->

# Add sorting to a comment list with persistence across refresh

<!-- meta:begin -->
| Format | Difficulty | Asked at | Round | Topics | Last reported |
| --- | --- | --- | --- | --- | --- |
| Algorithm | Easy | Bobyard | Online assessment | sorting | — |
<!-- meta:end -->

> **▶ [Solve it on TrueInterview](https://trueinterview.io/questions/9efb740a-713a-4618-aee6-a2161feaabc5)** — free, no card: a runnable editor, the sample and hidden tests, a judged verdict, and the reference solution.

## Problem

Build sorting behavior for a list of comments.

Each comment contains at least these fields:
- `id`: a string or integer
- `userId`: a string or integer
- `timestamp`: an ISO-formatted string or epoch milliseconds
- `content`: a string

Requirements:
1. Allow sorting by either `timestamp` or `userId`.
2. Offer both ascending (`asc`) and descending (`desc`) order.
3. Retain the sort field and direction selected by the user after a page reload, such as through a URL query parameter or `localStorage`.

Provide the central implementation in pseudocode or in the frontend framework of your choice, and explain:
- How the comparison logic works across strings, numbers, and time values.
- How the comments remain properly ordered when additional comments are received.

Constraints:
- The list can contain as many as N = 50,000 comments.

### Example
```text
Input: comments=[{id:1,userId:2,timestamp:1000},{id:2,userId:1,timestamp:2000}], sortBy=timestamp, order=asc
Output: order=[id:1,id:2]
```

The first comment comes before the second because its timestamp, 1000, is earlier than 2000 and the requested direction is ascending.

## Hints

<details>
<summary>Hint 1</summary>

Think about separating the state of the sort (field and direction) from the rendering logic, so the sort can be applied on initial load and changed later.

</details>

<details>
<summary>Hint 2</summary>

Use a URL query parameter (like `?sort=timestamp&order=asc`) or `localStorage` to persist the user's choice, and read it on page load to set the initial sort state.

</details>

<details>
<summary>Hint 3</summary>

Watch out for the edge case where the stored sort field no longer exists in the data, fall back to a default sort (e.g., by `timestamp` descending).

</details>

## Solution

The reference solution is on [the question page](https://trueinterview.io/questions/9efb740a-713a-4618-aee6-a2161feaabc5), beside an editor that runs your own attempt first. It is not reproduced here — try the problem before you read the answer.

## Asked at

[Bobyard](../../../companies/bobyard/README.md)

---

<sub>From [TrueInterview](https://trueinterview.io) — the free part of its question bank and company interview guides, synced every hour. Text licensed [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/): share or adapt it anywhere, crediting TrueInterview with a link back ([how](../../../LICENSE-CONTENT.md)). [Report a problem with this page](https://github.com/kevin-2023-code/Tech-Interview-Questions/issues/new?template=correction.yml).</sub>
