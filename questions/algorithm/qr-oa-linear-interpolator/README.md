<!-- Generated by scripts/sync.py. Do not edit by hand: the next sync overwrites it. -->

# Linear Interpolation

<!-- meta:begin -->
| Format | Difficulty | Asked at | Round | Topics | Last reported |
| --- | --- | --- | --- | --- | --- |
| Algorithm | Medium | Two Sigma | Online assessment | binary-search, math | May 2026 |
<!-- meta:end -->

> **▶ [Solve it on TrueInterview](https://trueinterview.io/questions/qr-oa-linear-interpolator)** — free, no card: a runnable editor, the sample and hidden tests, a judged verdict, and the reference solution.

## Problem

You are given a collection of 2D points via the array `points`, where several points can share the same x-coordinate. Use these points to define a piecewise linear function. For each value in the array `queries`, compute the interpolated y-value according to these rules:

- When the query's x-value equals an existing x-coordinate, the result is the maximum y among all points with that x.
- When the query lies strictly between two distinct x-coordinates, linearly interpolate using the largest y at the smaller x and the smallest y at the larger x.
- When the query is smaller than the minimum x-coordinate present, extrapolate using the segment formed by the two smallest distinct x-coordinates, each taken with its smallest y.
- When the query is larger than the maximum x-coordinate present, extrapolate using the segment formed by the two largest distinct x-coordinates, each taken with its largest y.

Return the results in the same order as the queries.

**Constraints:**
- 2 ≤ `points.length` ≤ 10^5
- 1 ≤ `queries.length` ≤ 10^5
- There are at least **two distinct** x-values in `points`
- Answers within 10^{-3} of the correct value are accepted

**Example 1:**
> **Input:** points = [[1, 2], [1, 6], [3, 8], [5, 4]], queries = [1, 2, 4, 0, 6]
> **Output:** [6.0, 7.0, 6.0, -1.0, 2.0]
> **Explanation:**
> - For x = 1, exact match, so return high(1) = 6.0.
> - For x = 2, interpolate between (1, 6) and (3, 8) → 7.0.
> - For x = 4, interpolate between (3, 8) and (5, 4) → 6.0.
> - For x = 0, left extrapolation with (1, 2) and (3, 8) → -1.0.
> - For x = 6, right extrapolation with (3, 8) and (5, 4) → 2.0.

**Example 2:**
> **Input:** points = [[0, 0], [2, 4]], queries = [1]
> **Output:** [2.0]

**Example 3:**
> **Input:** points = [[1, 2], [1, 5], [3, 9]], queries = [1, 2]
> **Output:** [5.0, 7.0]

## Examples

**Example 1**

```text
Input:
4
0.0 0.0
1.0 1.0
2.0 0.0
3.0 1.0
5
0.5
1.5
2.5
-1.0
4.0
Output:
0.5
0.5
0.5
-1.0
2.0
```

**Example 2**

```text
Input:
2
0.0 0.0
10.0 10.0
3
0.0
10.0
5.0
Output:
0.0
10.0
5.0
```

## Hints

<details>
<summary>Hint 1</summary>

When multiple points share the same x, only the maximum y at that x represents the function, when between points, you need the largest y at left x and smallest y at right x.

</details>

<details>
<summary>Hint 2</summary>

First, preprocess the points to create a map from x to the maximum y for that x, then sort the unique x-values, for queries, use binary search to find the relevant interval.

</details>

<details>
<summary>Hint 3</summary>

For a query beyond the data range, you may need to return the y of the nearest endpoint or use extrapolation, the problem statement may specify that case as returning that endpoint's value.

</details>

## Solution

The reference solution is on [the question page](https://trueinterview.io/questions/qr-oa-linear-interpolator), beside an editor that runs your own attempt first. It is not reproduced here — try the problem before you read the answer.

## Asked at

[Two Sigma](../../../companies/two-sigma/README.md)

---

<sub>From [TrueInterview](https://trueinterview.io) — the free part of its question bank and company interview guides, synced every hour. Text licensed [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/): share or adapt it anywhere, crediting TrueInterview with a link back ([how](../../../LICENSE-CONTENT.md)). [Report a problem with this page](https://github.com/kevin-2023-code/Tech-Interview-Questions/issues/new?template=correction.yml).</sub>
